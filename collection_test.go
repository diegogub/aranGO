package aranGO

import(
  "testing"
)

func Test_Update(t *testing.T){
  /*
  db ,err := NewDatabase("test","http://localhost:8529","diego","test")
  db.Auth = true
  db.Unsafe = true
  db.Log = false

  if db != nil {
    err = db.Ping()
    log.Println(err)
  }
  //doc.Key = "peterete"
  err = db.Col("test").Replace("test",223)
  log.Println("error de replace:",err)
  */
}
